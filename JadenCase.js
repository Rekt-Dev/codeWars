String.prototype.toJadenCase = function (str) {
    
      for (i=0;i<str.length;i++)
           
           console.log(str)
  return str

};


let result = function(`How can mirrors be real if our eyes aren/'t real`)
console.log(result)


function hasWhiteSpace(s) {
    return /\s/g.test(s);
  }